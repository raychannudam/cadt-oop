str1.equals(str2) // true or false
str1.qeualsIgnoreCase(str2) // true or false
str1.compareTo(str2) // equals to 0, bigger then or smaller then 0 (compare based on ascii value start from left to right character)
str.charAt(1) // return character at 1 index
str.toLowerCase()
str.toUpperCase()